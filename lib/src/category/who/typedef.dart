/// For ease access at WHO growth velocity measurement
typedef ZScorePercentile = ({num zScore, num percentile});

/// Simple [Record] for WHO growth velocity incremental
/// [low] and [high] represent month number cutoff
typedef VelocityMonths = ({int low, int high});
